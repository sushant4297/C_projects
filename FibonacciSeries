#include<stdio.h>
main()
{
    int N,i,a=-1,b=1,c;
    printf("Enter the length of the series to be printed: ");
    scanf("%d",&N);
    for(i=1;i<=N;i++)
    {
        c=a+b;
        printf("%d ",c);
        a=b;
        b=c;
    }
}
